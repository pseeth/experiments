{
  "num_frequencies": {"fill":"this in"},
  "num_mels": {"fill":"this in"},
  "sample_rate": {"fill":"this in"},
  "bidirectional": {"fill":"this in"},
  "hidden_size": {
    type: "int",
    default: 600,
    help: "Size of hidden layers (per layer?)"
  },
  "num_layers": {
    type: "int",
    default: 4,
    help: "Number of (hidden?) layers"
  },
  "embedding_size": {
    type: "int",
    default: 40,
    help: "Number of dimensions for embedding output"
  },
  "dropout": {
    "type": "float",
    "default": 0.3,
    "help": "Fraction of units to drop (per epoch?)"
  },
  "embedding_activation": {
    // TODO: are these choices or defaults?
    "default": ["sigmoid", "unitnorm"],
    "help": "?"
  },
  "projection_trainable": {
    "help": "Project to a learned input representation (learned from initial Mel projection)"
  },
  "rnn_type": {"fill":"this in"}
}
